{
  "name": "fbpost-checker-deploy",
  "version": "1.0.0",
  "description": "Firebase deploy with auto-backup",
  "main": "index.js",
  "scripts": {
    "backup": "node backup.js",
    "deploy": "npm run backup && firebase deploy",
    "test": "jest"
  },
  "dependencies": {
    "archiver": "^7.0.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
